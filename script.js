var button = document.getElementsByClassName('btn');
var input = document.getElementsByClassName('choice');
var mode = document.getElementById('add');

button.addEventListener('click', clickButton);

function createToDo() {
    var address = 
}